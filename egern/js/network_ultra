const resp = await $http.get("https://api.ip.sb/ip")
const ip = resp.body

return {
  type: "widget",
  padding: 12,
  children: [

    {
      type: "stack",
      direction: "row",
      children: [
        {
          type: "text",
          text: "当前IP",
          font: { size: 12 }
        },
        { type: "spacer" },
        {
          type: "text",
          text: ip,
          font: { size: 12 }
        }
      ]
    }

  ]
}
